# Controle de fluxo

## if / else if / else

```javascript
if (hasElement)
  appendToElement(element, node)
```

## switch / case

```javascript
switch (day)
  case 'SATURDAY':
  case 'SUNDAY':
    console.log('Weekend')
    break
  default:
    console.log('Work day')
```